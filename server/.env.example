# Database
DATABASE_URL="postgresql://username:password@localhost:5432/twistlist?schema=public"

# JWT Configuration
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"

# Server Configuration
PORT=3000
NODE_ENV=development

# CORS Configuration
# Local development
CORS_ORIGIN="http://localhost:3001"

# Production (Railway + Vercel)
# Set this to your Vercel domain (comma-separated for multiple domains)
# Example: CORS_ORIGIN="https://your-app.vercel.app,https://your-app-staging.vercel.app"

# Cookie Secret (for session management)
COOKIE_SECRET="your-random-cookie-secret-key"

# NOTE: When deploying to Railway:
# 1. Railway automatically provides DATABASE_URL if you use their PostgreSQL
# 2. Set NODE_ENV=production
# 3. Update CORS_ORIGIN to your Vercel domain
# 4. Generate strong secrets for JWT_SECRET and COOKIE_SECRET
